<Window x:Class="LawEditor.Views.LinkEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Linki redaktə et"
        Width="450" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="URL:" Foreground="White" Margin="0,0,0,6"/>

        <TextBox x:Name="UrlTextBox"
                 Grid.Row="1"
                 Padding="6"
                 FontSize="14"
                 Background="#2D2D2D"
                 Foreground="White"
                 BorderBrush="#3498DB"
                 CaretBrush="White"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Background="Red" Width="90" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="Save" Background="Green" Width="90" Click="SaveButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>